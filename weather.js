

var Tc = prompt ('Введи температуру  по цельсию : ');
Tf = (9/5)*Tc+32;
alert('По цельсию это будет: ' + Tf);

var name = prompt('Введите имя: ')
    if (name == 'Василий') alert ( 'Администратора зовут' + name )

var admin = name 
alert ('Как зовут администратора  ' + admin)